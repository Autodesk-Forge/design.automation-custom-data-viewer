module.exports = {

    // Enter the AWS key
    aws_key: "",
    // Enter the AWS key value
    aws_secret: "",
    // Enter the region if applicable
	aws_region: "",
    // Enter the bucket name
    aws_s3_bucket:""
}
